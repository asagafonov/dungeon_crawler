mod progress {
  pub struct Progress {
    is_finished: bool,
    position: str,
    turns_taken: i32,
    monsters_killed: i32,
    traps_survived: i32,
    items_found: i32,
    score: i32,
  }

  impl Progress {
    fn calculate_progress(&self) -> Self {

    }
  }
}